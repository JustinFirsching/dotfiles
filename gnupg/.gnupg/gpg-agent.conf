# GPG Agent Configuration
# Uses graphical pinentry programs when available

# Cache settings
default-cache-ttl 3600
max-cache-ttl 7200

# Pinentry program - uses a wrapper script for cross-platform support
pinentry-program /home/justin/.gnupg/pinentry-auto
